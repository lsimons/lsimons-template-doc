# Vocabulary for Vale. Spelling is cspell's job (cspell-words.txt); this
# list serves two Vale rules only:
#
# 1. Vale.Terms enforces the casing of a plain word listed here, so the
#    names below are the canonical spellings ("Quarto", not "quarto", in
#    prose; code spans are skipped).
# 2. Vale adds every entry to the exceptions of rules that support them
#    (Google.Headings, write-good.TooWordy), so a name here may stay
#    capitalized in a sentence-case heading.
#
# A line is a Go regex matched against a single word. Regex entries do not
# enforce casing, so plain jargon does not belong here.
#
# Not GitHub: Vale.Terms would flag the hostname in `lsimons.github.io`,
# which appears as link text throughout.
# The repo name is the README heading; listing it lets Google.Headings keep
# it as written (scripts/init.mjs renames it with everything else).
lsimons-template-doc
Astro
Starlight
Quarto
Vale
Markdown
Dependabot
Merriweather
Beamer

# write-good.TooWordy exemptions: the rule lists these as wordy, and they
# are not. Regex entries, so casing is not enforced.
(?i)it is
